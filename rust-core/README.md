# rust-core

This is a Rust library that provides an API for use in the *devman* application clients. It is designed to be used with
UniFFI to expose the API to a Jetpack Compose application.

# Architecture

- UniFFI is a tool that allows Rust libraries to be used from other languages by generating bindings automatically.